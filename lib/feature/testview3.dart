import 'package:flutter/material.dart';

class Testview3 extends StatelessWidget {
  const Testview3({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Testview3'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}